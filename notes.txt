How to create jsp ? 
	right click on views folder ->  new -> other -> type -> jsp -> next -> give jsp name and finish 
	
	
How To create  controller ? 
	1) right click on controller folder -> new -> other -> type class -> next -> Give class name and finish 
	2) use @Controller annotation above the class name 
	
	
class =>
			1) variables 
			2) methods 

How to run project / start server ?
project -> right click -> run as -> spring boot application 



How to create entity ? 
	we have folder called entity inside grownited folder 
		if not then we have to create folder entity 
	now create class UserEntity inside entity folder 
	use @Entity and @Table(name="users") at class level annotation 
	add variable in your UserEntity like , userId, firstName etc. 
	
	every entity must have primary key
		to mark field as a primary key and auto_increment we will use following annotations 
		@Id
		@GeneratedValue(strategy = GenerationType.IDENTITY)
		Integer userId 
		



	
How to generate getters and setters? 
	in yout class do right click at blank space ,
	then click on source -> now click on generate getters and setters ->
	select all -> Generate. 
	
	
Hibernate -> java -> database -> 
		no need to write sql
		if you want you can write 
		
		
		JAVA 		OOP
		Hibernate 	ORM
		
		class ---> table 
		entity --> 
		class -> variable --> columns 
		object -> save -> record : insert 
		  
	
How to create Repository ? 
	we must have to create repostitory inside repository folder 
		so 1) we will create repository folder if we dont have 
		   2) right click on repository folder 
		   		click -> new -> other ->
		   		type interface -> interface click -> next
		   			name input -> UserRepository -> finish
		   			
		   3) use @Repository annotation 
		   4) extends JpaRepositor<T,ID>
		   						T -> Table's entity 
		   						Id -> primary key's data type 
		   						 			 
		   	  
		
4 , 5 , 6 
all Entity 
Repository 
Controller 	


1) CategoryEntity
2) CategoryRepository  
	
---------------------------------------------------------

@Entity 
@Table 
@Id
@Column
@GeneratedValue
@Controller 
@Getmapping 
@Postmapping 

---------------------------------------------------------


table : entity 
db query : repository 


insert() : save() 
select() : findAll()


--------------------------------------------------
template -> 
https://youtu.be/HBgmpPl89lg
-----------------------------------------------------------------------

session => HttpSession

(HttpSession session)

data add -> setAttribute("dataName",data); 
	session.setAttribute("user",dbUser); 
	
destroy => invalidate() 
	session.invalidate()
	

jsp -> ${sessionScope.user.firstName}

<img src="${sessionScope.user.profilePicURL}" /> 
	
----------------------------------------------------------------------
session mamangement :- 
https://us02web.zoom.us/rec/share/0NMxoSvrCorTl3QSNucOk1DTZwdxFTLMEjoPs3xuo4WPfkqueP79DLHYkaynmvOf.ZJEng5YZWEeziFkH 
Passcode: cr!eFTE4

----------------------------------------------------------------------





















	
	
	
	
	
	
	
	
	
